# .github
Cinebench Benchmark scores a CPU you own with named Cinema 4D style runs so the graph stays easy to read on a duty host. Cinebench Cpu Benchmark keeps the result listed, so Cinebench Cpu Test stays a log you can still reverse on that host each day.
